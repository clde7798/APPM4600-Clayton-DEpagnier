This Repo contains my labs and homework diredctories. The sturcture is at the base level we have "Labs" and "Homework" directories. In the "Labs" directories there is a sub directory called "Lab1" contain the file from "Lab1"

Each time you should use:
git add <filename>
git commit -m "Comment"
git push

Commit 3

Commit 4